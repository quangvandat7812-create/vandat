--// LOGIN SYSTEM VANDATDZ (FULL)

local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer

-- FILE SAVE
local FILE = "vandatdz_key.txt"

-- KEY SETTINGS
local VALID_KEY = "vandatdz"
local KEY_DURATION = 24 * 60 * 60 -- 24h

-- LINK GET KEY
local GET_KEY_LINK = "https://your-link.com" -- 👉 đổi link tại đây

-- SAVE / LOAD
local HttpService = game:GetService("HttpService")

local function saveData(data)
    if writefile then
        writefile(FILE, HttpService:JSONEncode(data))
    end
end

local function loadData()
    if readfile and isfile and isfile(FILE) then
        return HttpService:JSONDecode(readfile(FILE))
    end
    return nil
end

-- GUI
local ScreenGui = Instance.new("ScreenGui", game.CoreGui)

local Frame = Instance.new("Frame", ScreenGui)
Frame.Size = UDim2.new(0,300,0,220)
Frame.Position = UDim2.new(0.5,-150,0.5,-110)
Frame.BackgroundColor3 = Color3.fromRGB(20,20,20)

local Title = Instance.new("TextLabel", Frame)
Title.Size = UDim2.new(1,0,0,40)
Title.Text = "Login Vandatdz"
Title.TextColor3 = Color3.new(1,1,1)
Title.BackgroundTransparency = 1
Title.TextScaled = true

local Box = Instance.new("TextBox", Frame)
Box.Size = UDim2.new(0.8,0,0,35)
Box.Position = UDim2.new(0.1,0,0.3,0)
Box.PlaceholderText = "Enter Key..."
Box.BackgroundColor3 = Color3.fromRGB(30,30,30)
Box.TextColor3 = Color3.new(1,1,1)

local Button = Instance.new("TextButton", Frame)
Button.Size = UDim2.new(0.8,0,0,35)
Button.Position = UDim2.new(0.1,0,0.55,0)
Button.Text = "LOGIN"
Button.BackgroundColor3 = Color3.fromRGB(0,170,255)
Button.TextColor3 = Color3.new(1,1,1)

-- 🔥 NÚT GET KEY
local GetKey = Instance.new("TextButton", Frame)
GetKey.Size = UDim2.new(0.8,0,0,30)
GetKey.Position = UDim2.new(0.1,0,0.75,0)
GetKey.Text = "GET KEY"
GetKey.BackgroundColor3 = Color3.fromRGB(50,50,50)
GetKey.TextColor3 = Color3.new(1,1,1)

local Status = Instance.new("TextLabel", Frame)
Status.Size = UDim2.new(1,0,0,20)
Status.Position = UDim2.new(0,0,1,-20)
Status.BackgroundTransparency = 1
Status.TextColor3 = Color3.fromRGB(255,0,0)

-- DRAG
local dragging, dragInput, dragStart, startPos

local function update(input)
    local delta = input.Position - dragStart
    Frame.Position = UDim2.new(
        startPos.X.Scale,
        startPos.X.Offset + delta.X,
        startPos.Y.Scale,
        startPos.Y.Offset + delta.Y
    )
end

Frame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = Frame.Position

        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

Frame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement then
        dragInput = input
    end
end)

UIS.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        update(input)
    end
end)

-- LOGIN
local function successLogin()
    Status.TextColor3 = Color3.fromRGB(0,255,0)
    Status.Text = "Login Success!"

    saveData({
        key = VALID_KEY,
        time = os.time()
    })

    wait(1)
    Frame:Destroy()

    print("RUN MAIN SCRIPT")
end

local function doLogin(key)
    if key == VALID_KEY then
        successLogin()
    else
        Status.Text = "Wrong Key!"
    end
end

Button.MouseButton1Click:Connect(function()
    doLogin(Box.Text)
end)

-- 🔥 GET KEY CLICK
GetKey.MouseButton1Click:Connect(function()
    if setclipboard then
        setclipboard(GET_KEY_LINK)
        Status.Text = "Link copied!"
    end

    if syn and syn.request then
        syn.request({Url = GET_KEY_LINK, Method = "GET"})
    end
end)

-- AUTO LOGIN
local data = loadData()
if data and data.key == VALID_KEY then
    if os.time() - data.time < KEY_DURATION then
        successLogin()
    end
end
