-- 🍌 Banana Hub FULL Demo (SAFE)
-- For YOUR OWN Roblox Game Only

-- SERVICES
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CollectionService = game:GetService("CollectionService")

local player = Players.LocalPlayer
local char = player.Character or player.CharacterAdded:Wait()
local hrp = char:WaitForChild("HumanoidRootPart")

-- STATES
_G.AutoFarm = false
_G.AutoQuest = false

-- ================= UI =================
local gui = Instance.new("ScreenGui", player.PlayerGui)
gui.ResetOnSpawn = false

local main = Instance.new("Frame", gui)
main.Size = UDim2.new(0, 430, 0, 270)
main.Position = UDim2.new(0.5,-215,0.5,-135)
main.BackgroundColor3 = Color3.fromRGB(25,25,25)
main.Active = true
main.Draggable = true
Instance.new("UICorner", main).CornerRadius = UDim.new(0,12)

local title = Instance.new("TextLabel", main)
title.Size = UDim2.new(1,0,0,40)
title.Text = "🍌 Banana Hub | FULL DEMO"
title.Font = Enum.Font.GothamBold
title.TextSize = 18
title.TextColor3 = Color3.fromRGB(255,221,70)
title.BackgroundTransparency = 1

local tabs = Instance.new("Frame", main)
tabs.Position = UDim2.new(0,0,0,40)
tabs.Size = UDim2.new(0,120,1,-40)
tabs.BackgroundColor3 = Color3.fromRGB(30,30,30)

local content = Instance.new("Frame", main)
content.Position = UDim2.new(0,120,0,40)
content.Size = UDim2.new(1,-120,1,-40)
content.BackgroundColor3 = Color3.fromRGB(35,35,35)

local function list(p)
	local l = Instance.new("UIListLayout", p)
	l.Padding = UDim.new(0,8)
	l.HorizontalAlignment = Enum.HorizontalAlignment.Center
end

list(tabs)

local pages = {}
local function page(name)
	local f = Instance.new("Frame", content)
	f.Size = UDim2.new(1,0,1,0)
	f.Visible = false
	f.BackgroundTransparency = 1
	list(f)
	pages[name] = f
	return f
end

page("Farm")
page("Quest")
page("Teleport")
pages.Farm.Visible = true

local function tab(text, p)
	local b = Instance.new("TextButton", tabs)
	b.Size = UDim2.new(1,-10,0,36)
	b.Text = text
	b.Font = Enum.Font.GothamBold
	b.TextSize = 14
	b.TextColor3 = Color3.new(1,1,1)
	b.BackgroundColor3 = Color3.fromRGB(45,45,45)
	Instance.new("UICorner", b).CornerRadius = UDim.new(0,8)
	b.MouseButton1Click:Connect(function()
		for _,v in pairs(pages) do v.Visible = false end
		pages[p].Visible = true
	end)
end

tab("🌾 Farm","Farm")
tab("📜 Quest","Quest")
tab("🗺 Tele","Teleport")

local function toggle(p,text,func)
	local s=false
	local b = Instance.new("TextButton", p)
	b.Size = UDim2.new(0.9,0,0,36)
	b.Text = text.." : OFF"
	b.Font = Enum.Font.Gotham
	b.TextSize = 14
	b.TextColor3 = Color3.new(1,1,1)
	b.BackgroundColor3 = Color3.fromRGB(60,60,60)
	Instance.new("UICorner", b).CornerRadius = UDim.new(0,8)
	b.MouseButton1Click:Connect(function()
		s = not s
		b.Text = text.." : "..(s and "ON" or "OFF")
		func(s)
	end)
end

-- ================= AUTO FARM =================
local function getEnemy()
	local near,dist=nil,math.huge
	for _,e in pairs(CollectionService:GetTagged("Enemy")) do
		if e:FindFirstChild("Humanoid") and e.Humanoid.Health>0 then
			local d=(hrp.Position-e.HumanoidRootPart.Position).Magnitude
			if d<dist then dist=d near=e end
		end
	end
	return near
end

RunService.Heartbeat:Connect(function()
	if _G.AutoFarm then
		local e = getEnemy()
		if e then
			hrp.CFrame = e.HumanoidRootPart.CFrame * CFrame.new(0,0,5)
			e.Humanoid:TakeDamage(10)
		end
	end
end)

toggle(pages.Farm,"Auto Farm",function(v) _G.AutoFarm=v end)

-- ================= AUTO QUEST (DEMO) =================
toggle(pages.Quest,"Auto Quest",function(v) _G.AutoQuest=v end)

-- ================= TELEPORT =================
local function tpBtn(p,text,pos)
	local b=Instance.new("TextButton",p)
	b.Size=UDim2.new(0.9,0,0,36)
	b.Text=text
	b.Font=Enum.Font.Gotham
	b.TextSize=14
	b.TextColor3=Color3.new(1,1,1)
	b.BackgroundColor3=Color3.fromRGB(60,60,60)
	Instance.new("UICorner",b).CornerRadius=UDim.new(0,8)
	b.MouseButton1Click:Connect(function()
		hrp.CFrame = CFrame.new(pos)
	end)
end

tpBtn(pages.Teleport,"Island 1",Vector3.new(0,5,0))
tpBtn(pages.Teleport,"Island 2",Vector3.new(100,5,100))

print("🍌 Banana Hub FULL Loaded")
